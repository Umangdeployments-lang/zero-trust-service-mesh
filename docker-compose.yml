version: "3.8"
services:
  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      - "8000:8000"
  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "3000:80"